[project]
name = "dsai-capstone-backend"
version = "0.1.0"
description = "Job Recommendation & Skill Gap Analysis API"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
